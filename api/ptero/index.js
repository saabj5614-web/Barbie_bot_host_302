const {get,post}=require('./client');const {requireOwner}=require('../../lib/auth');
module.exports=async(req,res)=>{try{if(!requireOwner(req,res))return;const action=String(req.query?.action||'dashboard').toLowerCase();
if(action==='dashboard'||action==='servers'||action==='nodes'){const [servers,nodes]=await Promise.all([get('/api/application/servers?per_page=100&include=allocations'),get('/api/application/nodes?per_page=100')]);return res.json({ok:true,servers,nodes});}
if(action==='power'){if(req.method!=='POST')return res.status(405).json({ok:false,error:'POST required'});const {id,signal}=req.body||{};if(!id||!['start','stop','restart','kill'].includes(signal))return res.status(400).json({ok:false,error:'Invalid server power request'});await post(`/api/client/servers/${encodeURIComponent(id)}/power`,{signal});return res.json({ok:true});}
if(action==='users')return res.json({ok:true,data:await get('/api/application/users?per_page=100')});return res.status(400).json({ok:false,error:'Unknown action'});}catch(e){console.error('PTERO API ERROR:',e);return res.status(e.status||500).json({ok:false,error:e.message||'Pterodactyl connection failed'});}};
